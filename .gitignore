node_modules/
dist/
dist-ssr/
*.local

# Bun lockfile — projeto usa npm/package-lock.json no Vercel
bun.lock

# Vercel CLI local
.vercel/

.env
.env.local
data/*.db
data/*.db-shm
data/*.db-wal
public/uploads/products/*
!public/uploads/products/.gitkeep

# Supabase CLI local state (link config, tokens) — must not be committed
supabase/.temp/
